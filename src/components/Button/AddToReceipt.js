import React from 'react';
import Button from 'material-ui/Button';

const AddToReceiptButton = (props) => (
    <Button color='primary' {...props}>Add To Receipt</Button>
);

export default AddToReceiptButton;